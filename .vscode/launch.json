{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Clang++ 调试当前文件 (LLDB)",
            "type": "lldb",
            "request": "launch",
            // ✅ 这里的名字必须和你 tasks.json 里的 label 完全一致
            "preLaunchTask": "C/C++: clang++.exe 生成活动文件", 
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "args": [],
            "cwd": "${fileDirname}",
            "terminal": "integratedTerminal"
        }
    ]
}