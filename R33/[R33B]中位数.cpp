#include <bits/stdc++.h>
using namespace std;

int T;
int n, s, a[100010];

int main() {
    cin >> T;
    for(; T--; ) {
        cin >> n >> s;
        for(int i = 1; i <= n; i ++) {
            cin >> a[i];
        }
        
    }
}