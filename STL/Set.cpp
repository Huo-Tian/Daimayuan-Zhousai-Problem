#include <bits/stdc++.h>
using namespace std;

vector<int> v{101, 110, 110, 3};

int main() {
    //set<int> x(v.begin(), v.end());
    //x.insert(), x.erase();
    //x.count(y);       查询是否存在
    //x.find(y);        找不到返回v.end();
    //x.lower_bound(y); 查找 <= y的元素
    //for(auto y : x) {
    //}
}