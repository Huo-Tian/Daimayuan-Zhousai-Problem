#include <bits/stdc++.h>
using namespace std;

/*
vector<int> v(3, 100);
vector<int> v{3, 100, 101};

*/
int main() {
    /*
    v.size();
    v.empty();
    v.clear();
    v.push_back(1);
    v.pop_back();
    
    迭代器:
    v.begin();
    v.end();

    v.insert(v.begin() + 1, 2);
    v.erase(v.begin + 2);

    lower_bound(v.begin(), v.end(), 100); >= 100 的 第一个位置
    upper_bound(v.begin(), v.end(), 100); > 100  的 第一个位置

    v.resize(100);
    for(int x : v) {
        cout << x << endl;
    }
    */
}