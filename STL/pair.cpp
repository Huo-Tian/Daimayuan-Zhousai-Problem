#include <bits/stdc++.h>
using namespace std;

pair<int, int> a;
//数对

int main() {
    // pair<int, int> b(3, 5); 直接赋值(A)
    // a.second = 7; / a.first = 9 单个直接赋值
    // a = make_pair(4, 6); 直接赋值(B)
    /*
    a < b ?
    a.first < b.first || (a.first == b.first && a.second < b.second)
    */
    // cout << a.first << " " << a.second << endl; 输出
}